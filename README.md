# Beeweat — Le api del tempo
Mille occhi, un solo cielo.

Prototipo React (Vite). Comandi:
- `npm install` — installa le dipendenze
- `npm run dev` — avvia in locale
- `npm run build` — build di produzione (usata da Vercel)

Le chiavi Supabase vanno inserite in `src/beeweat-supabase.js`.
